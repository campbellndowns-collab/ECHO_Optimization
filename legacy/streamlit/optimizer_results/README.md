# Optimizer results (runtime; gitignored contents)

Optional regression reference from a local run:

`drone_optimizer_integrated/`

Typical files:

- `physical_design_pool.csv`
- `physical_design_pool_metadata.json`
- `integrated_real_component_results.csv`
- `RUN_SUMMARY.txt`
- `pareto_frontier.csv`
- `rejection_summary.csv`
- `exception_details.csv`
- `run_settings.json`

Derived data only — not the authoritative engineering source.

Private runtime data is currently present locally but excluded from git.
