# Component data (runtime; gitignored contents)

Place here (gitignored):

- `component_warehouse.sqlite`
- `propulsion_performance_cache.sqlite` (optional derived cache)

These are expected by v0.7 at:

`PROJECT_ROOT/component_data/...`

where `PROJECT_ROOT` is this `legacy/streamlit/` directory.

Private runtime data is currently present locally but excluded from git.
